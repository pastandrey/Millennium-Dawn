UN_voting_category = {
	UN_SC_voting_mission = {
		icon = EU_voting_decision
		allowed = { always = yes }
		available = { un_sc_voting_result_trigger = yes }
		activation = { always = no }
		is_good = yes
		visible = {
			has_country_flag = has_sc_mission
		}
		days_mission_timeout = 5
		selectable_mission = yes
		complete_effect = {
			security_council_vote_finished = yes
			clr_country_flag = has_sc_mission
			set_country_flag = {
				flag = un_sc_vote_cooldown
				days = 45
				value = 1
			}
		}
		timeout_effect = {
			security_council_vote_finished = yes
			clr_country_flag = has_sc_mission
			set_country_flag = {
				flag = un_sc_vote_cooldown
				days = 45
				value = 1
			}
		}
		ai_will_do = { factor = -1000 }
	}
	UN_SC_voting_mission_result_timer = {
		icon = EU_voting_decision
		allowed = { always = yes }
		available = { always = no }
		activation = { always = no }
		is_good = yes
		visible = {
			has_global_flag = current_ongoing_sc_vote
		}
		days_mission_timeout = 5
		selectable_mission = yes
		complete_effect = { }
		timeout_effect = {
			remove_mission = UN_SC_voting_mission_result_timer
			set_country_flag = {
				flag = un_sc_vote_cooldown
				days = 45
				value = 1
			}
		}
		ai_will_do = { factor = -1000 }
	}
	UN_GA_voting_mission = {
		#icon = EU_voting_decision
		allowed = { always = yes }
		available = { un_ga_2_3_required = yes }
		activation = { always = no }
		is_good = yes
		visible = {
			has_country_flag = has_ga_mission
		}
		days_mission_timeout = 5
		selectable_mission = yes
		complete_effect = {
			general_assembly_vote_finished = yes
			clr_country_flag = has_ga_mission
		}
		timeout_effect = {
			general_assembly_vote_finished = yes
			clr_country_flag = has_ga_mission
		}
		ai_will_do = { factor = -1000 }
	}
	UN_GA_voting_mission_result_timer = {
		#icon = EU_voting_decision
		allowed = { always = yes }
		available = { always = no }
		activation = { always = no }
		is_good = yes
		visible = {
			has_global_flag = current_ongoing_ga_vote
		}
		days_mission_timeout = 5
		selectable_mission = yes
		complete_effect = { }
		timeout_effect = { remove_mission = UN_GA_voting_mission_result_timer }
		ai_will_do = { factor = -1000 }
	}
	UNSC_end_all_offensive_wars = {
		allowed = { always = yes }
		available = { has_offensive_war = no }
		activation = { always = no }
		is_good = no
		visible = {
			has_country_flag = has_end_war_mission
		}
		days_mission_timeout = 210
		complete_effect = {
			clr_country_flag = has_end_war_mission
		}
		timeout_effect = {
			apply_united_nations_sanctions = yes
			clr_country_flag = has_end_war_mission
		}
		ai_will_do = { factor = -1000 }
	}
}